# `git add <file>`
将文件（位于工作区）提交到暂存区

# `git commit [-m "备注"]`
将暂存区的文件提交到仓库之中
>[!note]
>`git commit -am "备注"`是git add .和git commit的结合版

# `git log`
查看提交记录
>[!note]
>`git log --oneline`  //查看简洁的提交记录
>`git ls-files`//查看已跟踪的文件

