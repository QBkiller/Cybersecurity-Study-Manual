# `git branch`
查看该仓库内的分支列表

# `git branch <branch-name>`
创建相应的分支

# `git switch <branch-name>`
切换到相应的分支

# `git merge <branch-name>`
将相应的分支合并至主分支

# `git branch -d <branch-name>`
删除已经合并过的分支
>[!note]
>若要删除未被合并的分支，请将==`-d`==改为==`-D`==

# `git log --graph --oneline --decorate --all`
在终端内查看分支图
