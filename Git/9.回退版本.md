![[Pasted image 20250810084930.png]]

>[!note]
>该命令后需要接版本，上一个版本可用HEAD^

>[!warning]
>git里面所有的操作都是可以回溯的
>使用git reflog可以查看回退后误操作的版本

# `git reset --soft 版本`
回退至相应的版本，保留**暂存区**和**工作区**的所有内容

# `git reset --hard 版本`
回退至相应的版本，**所有内容都不保存**

# `git reset --mixed 版本`
回退至相应的版本，只保留**工作区**的所有内容