>[!warning]
>==只有文件或文件夹所属用户或root用户才能使用==

`chmod 【-R】权限 文件或文件夹 `（用于修改权限细节）
-R对文件夹中的所以文件进行相同应用
权限处填写u=rwx,g=rwx,o=rwx或快捷写法

>[!warning]
>==只有root用户才能使用==

`chown 【-R】【用户】【：】【用户组】文件或文件夹`
-R对文件夹中的所以文件进行相同应用

