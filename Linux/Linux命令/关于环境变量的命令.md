环境变量是一组信息记录，用于操作系统运行的时候记录关键信息

env命令查看当前系统配置的环境变量信息

`$`符号可以去除环境变量的值

环境变量PATH会记录一组目录，目录之间用：隔开，记录的是命令的搜索；路径。

修改环境变量：
	临时修改：export 环境变量赋值
	永久生效：
		针对用户：~/.bashrc文件中配置
		针对全部用户：./erc/profile文件中配置
		最后通过：source 配置文件 ，立刻生效