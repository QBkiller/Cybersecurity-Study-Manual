检查指定网络服务器是否联通
`ping 【-c num】IP或主机名`（-c，检查的次数，若不使用-c，则无限次数）

wget是非交互式的文件下载器，可以在命令行内下载网络文件
`wget 【-b】url`
（-b，可选，后台下载，将日志写入当前工作目录的wget-log文件
url，指下载链接）

curl可以发送http网络请求，用于下载文件，获取信息等
`curl 【-o】url`
（-o，用于下载文件，当url为下载链接时，可用
url，要发起请求的网络地址）




