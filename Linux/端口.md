端口分为两种，物理端口以及虚拟端口。
计算机之间的通讯，通过IP地址只能锁定计算机，无法锁定具体的程序
通过端口可以锁定计算机上具体的程序，进行程序间的沟通。

Linux系统可以支持65535个端口，分为3类进行使用：
* 公用端口1~1023：用于系统内置或常用知名程序的绑定使用
* 注册端口1024~49151：用于松散绑定使用（用户自定义）
* 动态端口49152~65535：用于临时使用
***
* nmap IP地址  用于查看指定IP的对外暴露端口
* netstat -anp | grep 端口号 查看指定端口占用情况




