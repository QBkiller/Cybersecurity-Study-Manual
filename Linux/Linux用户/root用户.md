就是超级管理员账户

切换用户命令：su 【-】【用户名】
【-】可选，表示是否在切换用户后加载环境变量
用户名可省略，默认为root用户
切换用户后，通过exit命令退出到上一个用户，也可以用Ctrl+d

普通授权命令：sudo  其他命令（==需要为普通用户配置sudo认证==）

为普通用户配置sudo认证
1.切换到根用户，执行visudo，会自动通过vim编辑器打开/etc/sudoers
2.在文件的最后添加
【用户名】 ALL=ALL    NOPASSWD : ALL
3.通过:wq 保存
4.切换回普通用户





 
